# RonInfra

Two things live here:

- [`log_response/`](log_response/) — a runnable Python experiment measuring the
  log-contrast response of ImageNet-trained CNNs (see its
  [README](log_response/README.md) and [METHOD](log_response/METHOD.md)).
- [`Images/`](Images/), [`Misc/`](Misc/) — MATLAB utilities that others might
  find useful.
