# Core analysis pipeline (offline synthetic verification + fitting + figures)
numpy
matplotlib

# Real ImageNet-CNN back-end (TorchvisionModel). Pretrained weights are fetched
# by torchvision from download.pytorch.org, or pass a local state_dict via
# --weights if that host is unreachable.
torch
torchvision

# CLIP back-end (CLIPModel). Weights are fetched by open_clip from the HF hub /
# OpenAI mirrors; pass --weights with a local open_clip checkpoint if blocked.
open_clip_torch
